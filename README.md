Requirements Autohotkey

AutoHotkey files can be opened in Notepad or editors like VS Code, where you can view and edit the script to configure the keys that start and stop the macro. The key press interval is set to 60 ms by default, but you can adjust this value up or down depending on your needs; however, 60 ms is generally considered optimal for stable performance.
